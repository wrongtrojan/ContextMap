{# services/evaluate/prompts/evidence_evaluator.jinja2 #}
## 证据质量审计指令
你是一名学术审计员。请判断检索到的材料是否足以支撑对【原始问题】进行深度解答。

### 待审计信息
* **用户问题**: "{{ query }}"
* **当前重试次数**: {{ retry_index }}
* **当前检索结果**:
{% for doc in docs %}
[材料 {{ loop.index }}] 来源: {{ doc.metadata.asset_name or doc.metadata.asset_id }}, 页码/时间: {{ doc.metadata.page_label or doc.metadata.timestamp }}
内容摘要: {{ doc.content }}
---
{% endfor %}

### 审计逻辑：
1. **实质性匹配**：内容是否包含用户询问的核心公式、例题编号或关键变量描述？
2. **容错机制**：如果已搜到相关的物理背景和公式框架（即使不是例题原文），只要能够通过 LLM 推理补全，即视为合格。
3. **建议性输出**：仅给出 proceed/refetch 建议，由顶层编排决定是否重试。

### 输出 JSON 格式：
{
  "is_relevant": true,
  "confidence_score": 0.9,
  "missing_info": "说明缺失的部分，若无则为空",
  "missing_facets": ["缺失关键词1"],
  "suggested_keywords": ["建议追加检索词"],
  "action": "proceed",
  "need_sandbox": false,
  "need_visual": {{ "true" if any_video else "false" }}
}
